import { Component } from '@angular/core';

/** Temporary empty child for dashboard shell routes until feature pages exist. */
@Component({
  selector: 'app-shell-outlet',
  template: '',
  standalone: true,
})
export class ShellOutletComponent {}
